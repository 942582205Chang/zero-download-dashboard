# 0下载分析看板 · 使用说明

> 一份给「看数据的人」看的说明。不涉及技术实现，只讲：这个看板是什么、怎么打开、怎么看、怎么用。

---

## 一、这个看板是干什么的

简单说：**找出那些「发布了一段时间、却一直没人（前台）下载」的资源，帮你做预警。**

具体一点：

- 看板统计「**发布超过 15 天，前台下载次数还是 0**」的资源有多少。
- 然后按 学段 / 学科 / 品牌 / 场景 / 资源类型 / 版本 / 地区 等角度拆开，看问题集中在哪儿。
- 每一条 0 下载的资源，都能在明细页里查到具体是哪条、谁审核的、发布了多久。

> 一句话口径：**发布超过 15 天（严格大于 15 天），前台下载 = 0，就算一条「0 下载」。**

---

## 二、怎么打开、怎么登录

1. 用浏览器打开看板地址（收藏一下，以后直接点）。
2. 打开后先看到两个关口：
   - **地区校验**：只有 郑州 / 北京 / 江苏 地区能访问；不在范围会自动拦截。
   - **登录框**：输入账号密码（账号密码联系管理员获取）。
3. 登录成功后进入看板；同一天内浏览器会记住登录状态，关掉再开不用重新输。

> 右上角有「退出登录」，共用电脑时看完点一下更安全。

---

## 三、打开后第一眼能看到什么（首页）

首页从上到下分成几块，一次讲清楚：

### 1️⃣ 顶部一行数字卡片

一进来最先看到的 5 个小卡片，就是最关键的几个总数：

| 卡片 | 意思 |
|---|---|
| 资源总数 | 26-27 学年一共发布了多少资源 |
| **0 下载资源总数** | 发布超过 15 天、前台仍是 0 下载的资源有多少 |
| **整体占比** | 0 下载资源 ÷ 资源总数，算出来一个百分比 |
| B端 0 下载 | 发布超过 15 天的资源里，B 端下载为 0 的有多少 |
| C端 0 消费 | 发布超过 15 天的资源里，C 端消费为 0 的有多少 |

> 简单记：**卡片的分子都是「超过 15 天」的资源，只是看的渠道不同**——前台下载、B 端下载、C 端消费分别是三个不同渠道。

### 2️⃣ 七大报表区块（页面主体）

往下面滚，是七个大板块。左侧有一条小导航（返回顶部 / 各区块跳转 / 返回底部），点一下就能跳过去；窗口拉窄时导航会自动收起成一个小按钮。

**一、小初高整体** —— 全部资源按「发布天数」分档的统计总表。

**二、学段/品牌/场景/类型** —— 一张可以自由筛选、组合看数的表（下面单独讲怎么用）。

**三、各学段数据** —— 小学 / 初中 / 高中 三张表，分别看。

**四、各课程数据** —— 按语文、数学、英语……每个学科拆开看。

**五、各项目数据** —— 按品牌系列拆开看。

**六、各场景数据** —— 按场景拆开看。

**七、各资源类型数据** —— 按资源类型拆开看。

---

## 四、报表里的数字怎么理解

这些大表（一、三、四、五、六、七）长得都一样，都是「按发布天数分档」的统计表。理解一张就全懂了。

### 每一条「行」代表什么

- **整体**：这一大类的全部资源。
- **不超过 15 天**：刚发布、还没过观察期的资源。
- **已超过 15 天**：过了观察期的资源（**这行是关键**，0 下载预警主要看它）。
- 下面带 `- ` 前缀的行（如 ` - 语文`）：这一大类下面某个子类的「已超过 15 天」数据。

### 表里的「列」代表什么

表格的列分成两大组：**资源数量** 和 **消费数据**。

**资源数量**组里，最常用的是这几列：

| 列名 | 意思 |
|---|---|
| all | 这一类总共多少份资源 |
| 前台 0下载 | 其中前台下载为 0 的有多少份 |
| 整体占比 | 0 下载数 ÷ 资源总数，算百分比 |

**消费数据**组里，是三个渠道的实际消费数字（前台下载、B 端下载、C 端消费）以及它们的占比、平均值。

> 💡 **小技巧**：把鼠标悬停在任何一个「占比」或「平均值」的数字上，会弹出气泡，告诉你这个数是怎么算出来的（哪两个数相除），同时高亮参与计算的两个数字。看不懂一个百分比怎么来的，悬停一下就知道。

---

## 五、组合筛选表（第二块）怎么用

这是首页里唯一能「自己动手筛」的表。顶部有一排下拉框：

**学段、品牌、场景、类型、课程** 五个下拉 + 「✕ 清除」+「⬇ 导出 Excel」。

怎么用：

1. 在任意一个下拉里选一个值（比如 学段选「初中」），表格立刻只显示初中的数据。
2. **五个条件是叠加的**：可以同时选 学段=初中、场景=练习，看「初中 + 练习」的数据。
3. **下拉选项会联动**：选了某个条件后，其他下拉里只会列出「还能匹配上」的选项，不会出现选了也白选的空选项。
4. 选「课程」时，学段会自动跟着锁定（比如选了初中语文，学段自动变初中）。
5. 点「✕ 清除」一键清空所有条件。
6. 点「⬇ 导出 Excel」把当前筛选出来的表格存成 Excel 文件。

表格里每组数字的含义：

| 分组 | 说明 |
|---|---|
| 资源数量_all | 这个组合下**全部**资源的情况 |
| 资源数量_超过15天 | 这个组合下**已超 15 天**资源的情况 |
| 消费数据_all | 前台 / B 端下载、C 端消费的总和，以及「C端单份消费」（消费 ÷ 资源数，平均一份值多少钱） |

最下面一行「合计」是当前筛选结果的总和。

---

## 六、明细页（点「查看明细」进去）

首页右上角有个「📊 查看明细」，点开是单独一个页面：**所有 0 下载资源的逐条清单**。

这里能做几件事：

### 1. 搜索
顶部搜索框，输入关键词即可。**可以搜任意一个字段**（标题、品牌、场景、类型、版本……都行）；多个关键词用空格隔开，要求全部命中。

### 2. 筛选
搜索框右边有一排下拉，可以按 **课程、品牌、场景、类型、审核人、年级、版本、地区** 筛选，还能按 **发布天数分段**（16-30 天、31-60 天……）筛。

### 3. 排序
点任意一列表头即可按这列排序，再点一次切换升序/降序（表头箭头 ↑↓ 表示当前排序）。

### 4. 翻页
表格右上角选每页显示 50 / 100 / 200 条，前后翻页。

### 5. 怎么读这一行
- 前 3 列（课程、资源id、标题）是固定的，横向拉滚动条时一直能看到。
- **资源id 可以点**，直接跳转到那条资源的页面。
- 数据每 5 行有一道浅蓝底色，方便横向对着一行看。
- **发布天数** = 从审核通过到今天的自然天数，数字越大说明「挂越久还没人下」。

---

## 七、几个常见疑问

**Q：为什么有些资源「前台下载 0」但不被算进「0 下载」？**
A：因为还没满 15 天。刚发布的资源还在观察期内，不参与 0 下载预警，等超过 15 天仍是 0 才会被统计进来。

**Q：「整体占比」是怎么算的？**
A：0 下载资源总数 ÷ 资源总数。注意分母是「全部资源」，不是「超过 15 天的资源」。

**Q：数据多久更新一次？**
A：页面顶部左上角有「更新：×年×月×日 ×时×分」，就是数据的最新时间，对应最近一次取数。工作日每天会自动取几次数、更新几次。

**Q：首页数字和明细页对不上？**
A：正常的。明细页只列「超过 15 天仍 0 下载」的那部分资源；首页第一张表还包含「不超过 15 天」的资源，所以两边数量不同。
